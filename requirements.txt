#Claude generated requirements doc for other users
# Google OAuth + Gmail API (Steps 1-3)
google-auth-oauthlib
google-api-python-client

# Loads GROQ_API_KEY from .env (Steps 2-3)
python-dotenv

# Groq LLM client — for ambiguous-email classification (Step 2)
groq

# Progress bar (Step 3)
tqdm

# HTTP requests for unsubscribe URLs (Step 3)
requests

# Pretty tables in the terminal (Step 3)
rich
